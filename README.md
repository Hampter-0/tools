url_shortener genertates a random string of caracters that will save the inputted link and returns the link when u fetch it with the given id.

pasword generator will generate a random string of caracters and numbers of ur chosen length.

to use these tools u will need python3 installed:

check installation:
python --version
or
python3 --version

If not installed → download from python.org

how to run the program:

Open Command Prompt (Windows) or Terminal (macOS/Linux)
Navigate to the project folder or right click in the folder:

run the following command:

python main.py 
or
python3 main.py

or u can use the main.bat file included to run the program

made by ----- hampter -----


